// src/reducers/index.js

const initialState = {
  // Your initial state here
};

function rootReducer(state = initialState, action) {
  switch (action.type) {
    // You can add cases here later
    default:
      return state;
  }
}

export default rootReducer;
